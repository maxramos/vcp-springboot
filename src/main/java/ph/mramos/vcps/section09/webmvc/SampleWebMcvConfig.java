package ph.mramos.vcps.section09.webmvc;

//@Configuration // Uncomment when there's config to be added or updated.
//@EnableWebMvc // If NOT present then the default MVC config will NOT be overridden and instead this config will just add to it. Adding @EnableWebMvc stops the loading of the default config.
public class SampleWebMcvConfig /*implements WebMvcConfigurer*/ { // If extends WebMvcConfigurationSupport/DelegatingWebMvcConfiguration then must remove @EnableWebMvc. Extending either classes stops the loading of the default config.

}
